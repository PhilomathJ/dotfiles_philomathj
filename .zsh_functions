## combine mkdir & cd ##
mkcd () {
  mkdir "$1"
  cd "$1"
}

function largest () {
    echo "Listing the largest files/dirs in order..."
    du -h -x -s -- * | sort -r -h | head -20;
}

function hg() {
    history | grep "$1";
}
